insightface
onnxruntime
# GPU: pip uninstall onnxruntime && pip install onnxruntime-gpu (NVIDIA + CUDA only)
openvino   # Intel GPU (Iris Xe / Arc) backend; runs on CPU too. Add openvino-dev for omz_downloader.
opencv-python-headless
Pillow
numpy
scikit-learn
flask
psutil
# Belows are for one-time setup installer for Windows
platformdirs
pywebview
